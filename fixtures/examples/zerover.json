{
  "meta": {
    "name": "zerover",
    "description": "ZeroVer keeps the major version at 0 — breaking changes bump minor instead",
    "default_branch": "main"
  },
  "config": {
    "content": "{ \"versioning\": \"zerover\", \"package\": [ { \"name\": \"lib\", \"path\": \".\", \"versioned_files\": [{\"path\": \"version.toml\", \"format\": \"toml\"}] } ] }"
  },
  "packages": [
    { "name": "lib", "path": ".", "initial_version": "0.3.0", "tag": "v0.3.0" }
  ],
  "commits": [
    { "message": "feat!: breaking API change", "files": ["src/lib.rs"] }
  ],
  "expect": {
    "check_contains": ["lib", "0.4.0"],
    "check_not_contains": ["Nothing to release", "1.0.0"],
    "packages_released": 1
  }
}
